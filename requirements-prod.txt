# Production dependencies for Flask web server and AI predictions

# Web server
Flask>=2.3.0
Flask-CORS>=4.0.0
Flask-SQLAlchemy>=3.0.0
gunicorn>=21.2.0

# Data and ML libraries (CPU-only versions to fit free tier build memory limits)
numpy==1.26.4
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=1.0.0

# PyTorch CPU version (under 200MB, avoiding CUDA wheels of 2GB+)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.5.0

# Utilities and config
python-dotenv>=1.0.0
flask-socketio>=5.3.0
psutil>=5.9.0
jsonschema>=4.19.0
email-validator>=2.0.0
tqdm>=4.62.0
